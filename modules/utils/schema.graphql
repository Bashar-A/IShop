module.exports = `
    scalar DateTime

    enum SortOrder {
        ASC
        DESC
    }

    input DateOperators {
        eq: DateTime
        lt: DateTime
        lte: DateTime
        gt: DateTime
        gte: DateTime
    }

    input StringOperators {
        eq: String
        ne: String
        regex: String
        in: [String!]
        nin: [String!]
    }
`